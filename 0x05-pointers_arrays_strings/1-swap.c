#include "main.h"
#include <stdio.h>
#include <unistd.h>
/**
 * swap_int - swaps two integers
 * @a: integer to swap
 * @b: integer to swap
 */

void swap_int(int *a, int *b)
{
	int tmp;

	temp = *a;
	*a = *b;
	*b = temp;
}
